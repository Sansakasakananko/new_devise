<!DOCTYPE html>
<html>
  <head>
    <title>NewDevise</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <% if user_signed_in? %>
        <p> <%=link_to current_user.name ,mypage_path%>さん、ようこそ</p>
      <% end %>
    </header>
    
    <%= yield %>
  </body>
</html>
